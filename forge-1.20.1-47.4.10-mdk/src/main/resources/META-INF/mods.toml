# This is an example mods.toml file. It contains the data relating to the loading mods.
# There are several mandatory fields (#mandatory), and many more that are optional (#optional).
# The overall format is standard TOML format, v0.5.0.
# Note that there are a couple of TOML lists in this file.
# Find more information on toml format here:  https://github.com/toml-lang/toml
# The name of the mod loader type to load - for regular FML @Mod mods it should be javafml
modLoader="javafml" #mandatory
# A version range to match for said mod loader - for regular FML @Mod it will be the forge version
loaderVersion="${loader_version_range}" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
# The license for you mod. This is mandatory metadata and allows for easier comprehension of your redistributive properties.
# Review your options at https://choosealicense.com/. All rights reserved is the default copyright stance, and is thus the default here.
license="${mod_license}"
# A URL to refer people to when problems occur with this mod
#issueTrackerURL="https://change.me.to.your.issue.tracker.example.invalid/" #optional
# If your mod is purely client-side and has no multiplayer functionality (be it dedicated servers or Open to LAN),
# set this to true, and Forge will set the correct displayTest for you and skip loading your mod on dedicated servers.
#clientSideOnly=true #optional - defaults to false if absent
# A list of mods - how many allowed here is determined by the individual mod loader
[[mods]] #mandatory
# The modid of the mod
modId="${mod_id}" #mandatory
# The version number of the mod
version="${mod_version}" #mandatory
# A display name for the mod
displayName="${mod_name}" #mandatory
# A URL to query for updates for this mod. See the JSON update specification https://docs.minecraftforge.net/en/latest/misc/updatechecker/
#updateJSONURL="https://change.me.example.invalid/updates.json" #optional
# A URL for the "homepage" for this mod, displayed in the mod UI
#displayURL="https://change.me.to.your.mods.homepage.example.invalid/" #optional
# A file name (in the root of the mod JAR) containing a logo for display
#logoFile="metrogenesis.png" #optional
# A text field displayed in the mod UI
#credits="" #optional
# A text field displayed in the mod UI
authors="${mod_authors}" #optional
# Display Test controls the display for your mod in the server connection screen
# MATCH_VERSION means that your mod will cause a red X if the versions on client and server differ. This is the default behaviour and should be what you choose if you have server and client elements to your mod.
# IGNORE_SERVER_VERSION means that your mod will not cause a red X if it's present on the server but not on the client. This is what you should use if you're a server only mod.
# IGNORE_ALL_VERSION means that your mod will not cause a red X if it's present on the client or the server. This is a special case and should only be used if your mod has no server component.
# NONE means that no display test is set on your mod. You need to do this yourself, see IExtensionPoint.DisplayTest for more information. You can define any scheme you wish with this value.
# IMPORTANT NOTE: this is NOT an instruction as to which environments (CLIENT or DEDICATED SERVER) your mod loads on. Your mod should load (and maybe do nothing!) whereever it finds itself.
#displayTest="MATCH_VERSION" # if nothing is specified, MATCH_VERSION is the default when clientSideOnly=false, otherwise IGNORE_ALL_VERSION when clientSideOnly=true (#optional)

# The description text for the mod (multi line!) (#mandatory)
description='''${mod_description}'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.${mod_id}]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="${forge_version_range}" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the dependency is not mandatory
    # BEFORE - This mod is loaded BEFORE the dependency
    # AFTER - This mod is loaded AFTER the dependency
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT, or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.${mod_id}]]
    modId="minecraft"
    mandatory=true
    # This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

#[[dependencies.${mod_id}]]
#    modId="framework"
#    mandatory=true
#    versionRange="[0.6.0,)"
#    ordering="NONE"
#    side="BOTH"
# 代码未使用 Framework API，已移除依赖

#[[dependencies.${mod_id}]]
#    modId="geckolib"
#    mandatory=true
#    versionRange="[4.2,)"
#    ordering="NONE"
#    side="BOTH"
# 代码中未使用 GeckoLib API，已移除依赖

#[[dependencies.${mod_id}]]
#    modId="metrogenesis_structurize"
#    mandatory=true
#    versionRange="[1.0.816,)"
#    ordering="NONE"
#    side="BOTH"
# 【已移除】使用自研全息蓝图系统，无需前置模组

# Features are specific properties of the game environment, that you may want to declare you require. This example declares
# that your mod requires GL version 3.2 or higher. Other features will be added. They are side aware so declaring this won't
# stop your mod loading on the server for example.
#[features.${mod_id}]
#openGLVersion="[3.2,)"

# ═══ Structurize (本地嵌入，同 jar 共存) ═══════════════════════
[[mods]]
    modId="metrogenesis_structurize"
    version="${mod_version}"
    displayName="Structurize (MetroGenesis embed)"
    authors="LDT Team"
    description='''Structurize source code embedded locally, same jar as MetroGenesis.'''

[[dependencies.metrogenesis_structurize]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.metrogenesis_structurize]]
    modId="minecraft"
    mandatory=true
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

# ═══ MineColonies (本地嵌入，同 jar 共存) ═══════════════════════
[[mods]]
    modId="minecolonies"
    version="${mod_version}"
    displayName="MineColonies (embedded)"
    authors="LDT Team"
    description='''MineColonies source code embedded locally, same jar as MetroGenesis.'''

# ═══ Domum Ornamentum (本地嵌入，同 jar 共存) ═════════════════鈹€
[[mods]]
    modId="metrogenesis_domum"
    version="${mod_version}"
    displayName="Domum Ornamentum (MetroGenesis embed)"
    authors="LDT Team"
    description='''Domum Ornamentum source code embedded locally, same jar as MetroGenesis.'''

[[dependencies.metrogenesis_domum]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.metrogenesis_domum]]
    modId="minecraft"
    mandatory=true
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

# ═══ MineColonies 依赖声明 ═══════════════════════════════════
# ═════ structurize → 已内嵌源码，无需外部依赖
# blockui      → 自研版本 (com.metrogenesis.blockui)
# domum_ornamentum → 已内嵌源码
[[dependencies.minecolonies]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.minecolonies]]
    modId="minecraft"
    mandatory=true
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

# ═════ MrCrayfish 家具（已禁用 → 见 build.gradle 注释说明）═══════
# [[dependencies.${mod_id}]]
#     modId="framework"
#     mandatory=true
#     versionRange="[0.8.0,)"

# ═══ BlockUI (嵌入) ═══════════════════════════════════════════
[[mods]]
    modId="blockui"
    version="${mod_version}"
    displayName="BlockUI (embedded)"
    authors="LDT Team"
    description='''BlockUI XML GUI framework embedded in MetroGenesis.'''

[[dependencies.blockui]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.blockui]]
    modId="minecraft"
    mandatory=true
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"
#     ordering="NONE"
#     side="BOTH"
# 编译/运行时均已排除 framework，如有需要解除注释并同步 build.gradle

# [[dependencies.${mod_id}]]
#     modId="refurbished_furniture"
#     mandatory=false
#     versionRange="[1.0.20,)"
#     ordering="NONE"
#     side="BOTH"
# 编译/运行时均已排除 refurbished_furniture，如有需要解除注释并同步 build.gradle
